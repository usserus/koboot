import { createTheme } from '@rneui/themed';

const theme = createTheme({
  lightColors: {
    primary: '#234523',
    secondary: '#234523',
  },
  darkColors: {
    primary: '#000',
  },
  mode: 'light',
});

export default theme;