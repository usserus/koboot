import { createTheme } from '@rneui/themed';

const theme = createTheme({
  lightColors: {
    primary: '#0E5151',
    secondary: '#234523',
  },
  darkColors: {
    primary: '#000',
  },
  mode: 'light'
});

export default theme;