import { createTheme } from '@rneui/themed';

const theme = createTheme({
  lightColors: {
    primary: '#0E5151',
    secondary: '#E6E6E6',
    background: '#FFFFFF',
  },
  darkColors: {
    primary: '#000',
  },
  mode: 'light'
});

export default theme;